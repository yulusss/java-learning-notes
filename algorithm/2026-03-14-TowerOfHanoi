我对于汉诺塔的理解是：3根柱子，分别为A柱，B柱，C柱，目标任务是将A柱上的圆盘数 n 移动到C柱上，中途要借用到B柱，而且一次只能移动一个圆盘，同时大的圆盘必须在下面。

首先A柱的圆盘想要移动到C柱，就要先把最底下的圆盘移动到C柱，则上面的圆盘（也就是n - 1个圆盘）需要移动到B柱，这个过程需要借助到C柱，那么总结起来：

**第一步**需要将A柱作为源柱，即需要移动圆盘的柱子，移动n - 1个圆盘到B柱，要借助到C柱来移动，此时源柱是A柱，辅助是C柱，目标是移动到B柱。

**第二步**是把A柱上的最后一个圆盘移动到C柱上去，也就是直接移动即可。

**第三步**是将B柱上的圆盘全部移动到C柱上，此时B柱作为源柱，A柱作为辅助，C柱作为目标。

**当n = 1时**（即源柱只剩一个圆盘需要移动到目标柱了），直接移动即可，同时也是递归终止条件

```java
public class Hanoi {
    /**
     * n : 有几个盘子
     * from : 源地
     * to : 目标地
     * helper : 临时存放地
     */
    public static void hanoi(int n , char from , char to , char helper){

        //终止条件
        //现在只剩最后一个盘子，直接把它从 from 移到 to 就行
        if(n == 1){
            System.out.println("移1从" + from + "到" + to);
            return;
        }

        /*
          原来的参数：hanoi(3, A, C, B)
          from = A（从哪里拿）
          to = C（最终要去哪）
          helper = B（可以借用哪）
          现在的调用：hanoi(2, A, B, C)
          from = A（还是从A拿）
          to = B（目标变成B了！）
          helper = C（用C帮忙）
          为什么 to 变成 B 了？
          因为这一步的任务是：先把上面2个暂时放到B，不是直接放到C
         */
        hanoi(n - 1 , from , helper , to);
        System.out.println("移" + n + "从" + from + "到" + to);
        /*
        helper（原来的B）变成了新的 from（要从B拿）
        to 还是 C（最终目标不变）
        from（原来的A）变成了新的 helper（可以用A帮忙）
         */
        hanoi(n - 1 , helper , to , from);
    }
}
```
